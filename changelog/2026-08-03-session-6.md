# 2026-08-03 — Session 6

## Modified

- `index.html`
  - Added `.wordmark-row` and `.wordmark .wordmark-by` CSS rules (near line 131) for a small byline next to the header wordmark.
  - Header product bar now renders `CONGRESSWATCH` followed by a smaller, muted "By OpenSourcePatents" label on the same baseline.
  - Header wordmark changed to `CONGRESSWATCH.US`; added `.wordmark .wordmark-tld` rule so the `.US` suffix renders in `--accent-blue` — giving the wordmark a red / white / blue reading.
  - "By OpenSourcePatents" restyled to match the tagline exactly (`.pb-tagline` size/weight/color/tracking).
  - Moved "Open Source Government Accountability" out of the product bar and into the top `.osp-nav`, sitting beside the O.S.P. logo behind a divider; new `.nav-tagline` rule (`margin-right:auto`, hidden under 900px).
