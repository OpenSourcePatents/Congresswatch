# Session 1 -- 2026-03-31

## Critical Fixes
- Fixed score pipeline: `compute_score()` now implements full 6-signal weighted scoring (stock timing 25pts, wealth gap 25pts, donor-vote 20pts, bill authorship 15pts, travel 10pts placeholder, attendance 5pts)
- Fixed `fetch_finance.py` write-back: safe merge into `members.json` preserves fields from other pipelines instead of overwriting
- Fixed `fetch-data.yml` schedule: moved from 3:00 UTC to 1:00 UTC so member fetch runs BEFORE votes and finance pipelines
- Fixed `fetch-votes.yml`: added missing `permissions: contents: write` block
- Fixed `fetch.py` syntax error: removed markdown triple backticks from `infer_chamber()`, added proper indentation throughout, fixed "American Soma" typo to "American Samoa", removed debug file writing, added `__main__` guard
- Fixed `fetch-data.yml`: now calls `fetch.py` directly instead of duplicate inline Python (eliminates implementation drift)

## Frontend Fixes
- Removed fake `computeScore()` hash function -- shows "--" when no real score exists
- Removed fake `computeNetWorth()` -- shows "Financial disclosure pending" instead of fabricated numbers
- Removed hardcoded donor network placeholders ("PAC #1", "PAC #2", etc.) -- shows "Donor data pending" message
- Fixed patterns tab: only shows alerts for real data signals (ALEC matches, insider signals, missed votes, PAC ratios)
- Fixed `buildFlags()`: removed alerts generated from fake score thresholds
- Fixed FLAG FOR REVIEW button: now opens GitHub issue template with member name
- Fixed stats strip: correctly sums `corporate_insider_signals` across all members
- Added data freshness indicator (4th stat box showing last update date)
- Added data coverage stats to sidebar (scored count, finance count)
- Updated disclaimer text with complete methodology note
- Removed `loadDemo()` fake data -- shows honest empty state on data load failure
- All score displays handle null gracefully (show "--" with tooltip)
- Sort handles null scores (pushes to bottom)
- Filter handles null scores (excluded from score-based filters)
- Net worth charts only render when real data exists

## New Files
- `bootstrap.py` -- repo health check script (creates dirs, checks data state, prints summary)
- `data/ptr_source_manifest.json` -- empty PTR pipeline manifest (valid format, allows clean exit)
- `.github/workflows/bootstrap.yml` -- moved from repo root, now a proper workflow
- `changelog/2026-03-31-session-1.md` -- this file

## Files Modified
- `fetch_finance.py` -- new 6-signal `compute_score()`, safe merge write-back to members.json
- `fetch.py` -- complete rewrite fixing syntax errors, indentation, typos, debug artifacts
- `.github/workflows/fetch-data.yml` -- schedule 3:00->1:00 UTC, calls fetch.py instead of inline Python
- `.github/workflows/fetch-votes.yml` -- added permissions block
- `index.html` -- all frontend fixes listed above
- `CLAUDE.md` -- updated pipeline schedule, added changelog instruction, fixed Chart.js reference
