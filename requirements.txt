# CongressWatch — Python pipeline dependencies
# Install: pip install -r requirements.txt
# (CI workflows install per-pipeline subsets for speed; this is the full local set)
requests>=2.31.0
beautifulsoup4>=4.12.0
scikit-learn>=1.4.0
numpy>=1.26.0
pdfplumber>=0.11.0
pypdf>=4.0.0
